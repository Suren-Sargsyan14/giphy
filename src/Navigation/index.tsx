import React from 'react';
import { NavigationContainer } from '@react-navigation/native';

import MainRoot from './Roots/Main';

const Navigation = () => {
  return (
    <NavigationContainer>
      <MainRoot />
    </NavigationContainer>
  );
};

export default Navigation;
